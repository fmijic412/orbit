{
  "name": "orbit-runner",
  "private": true,
  "version": "0.1.22",
  "type": "module",
  "description": "A web-based 3D game built with Three.js, Vite, and TypeScript.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
